import { Button } from "govuk-react"
import styled from "styled-components"

const StyledSaveLinkButton = styled(Button)`
  margin-top: 0.94rem;
  margin-bottom: 0;
`

export { StyledSaveLinkButton }
