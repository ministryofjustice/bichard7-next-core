import styled from "styled-components"
import { tagBlue, gdsTagBlue } from "utils/colours"

export const ReportContainer = styled.div`
  h3 {
    margin: 0;
    padding: 1rem;
    background-color: ${tagBlue};
  }

  h4 {
    margin: 0;
    padding: 1rem;
    background-color: ${gdsTagBlue};
  }
`
