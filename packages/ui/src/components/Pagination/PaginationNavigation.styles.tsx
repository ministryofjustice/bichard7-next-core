import styled from "styled-components"

export const PaginationNav = styled.nav`
  justify-self: flex-end;
  align-self: flex-end;
  min-height: 50px;

  li.moj-pagination__item {
    margin-top: 8px;
  }
`
