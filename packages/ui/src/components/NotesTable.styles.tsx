import styled from "styled-components"
import { Table } from "components/Table"

export const StyledNotesTable = styled(Table)`
  th,
  td {
    font-size: var(--case-details-default-font-size);
  }
`
