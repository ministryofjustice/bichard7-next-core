{
  "name": "convert_spi_to_aho",
  "retryCount": 10,
  "retryLogic": "EXPONENTIAL_BACKOFF",
  "retryDelaySeconds": 10,
  "timeoutSeconds": 30,
  "responseTimeoutSeconds": 25,
  "timeoutPolicy": "RETRY",
  "inputKeys": ["correlationId", "s3Path"],
  "outputKeys": ["s3TaskDataPath", "auditLogRecord", "auditLogEvents", "error"]
}
