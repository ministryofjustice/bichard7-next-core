enum Workflow {
  BICHARD_PROCESS = "bichard_process",
  COMPARES_FILES = "compares-files",
  INCOMING_MESSAGE_HANDLER = "incoming-message-handler",
  RERUN_ALL = "rerun-all",
  RERUN_FAILURES = "rerun-failures"
}

export default Workflow
